# Bloquea el acceso directo al JSON desde el navegador.
# Solo los scripts PHP del servidor pueden leerlo/escribirlo.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
